# freqtrade is optional and should be installed in a separate strategy sandbox:
# 2024.1 depends on unavailable pandas-ta here; 2026.4 requires numpy>2.0 while
# the ML stack below is pinned to numpy 1.26.x for sklearn/scipy compatibility.
hvac==2.1.0
torch==2.2.1
xgboost==2.0.3
lightgbm==4.3.0
hmmlearn==0.3.2
scikit-learn==1.4.0
pandas==2.2.1
numpy==1.26.4
scipy==1.12.0
requests==2.31.0
joblib==1.3.2
python-dotenv==1.2.2
tqdm==4.66.0
scrapling>=0.4.1
mcp==1.27.1
openai==2.24.0
py-clob-client-v2==1.0.1
python-telegram-bot==22.0
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.12.5
pydantic-settings==2.14.1
streamlit==1.57.0
tenacity==9.1.4
web3==7.16.0
websockets==12.0
eth-abi==5.2.0
httpx==0.28.1
eth-account==0.13.7
pytest==8.0.0
pytest-asyncio==0.23.0
psutil
transformers
redis>=5.0.0
hiredis>=2.3.0
feedparser>=6.0.10
# duckdb
cryptography==42.0.5
yfinance
ccxt

# Token-efficient agent context tooling (referenced from graphify-out/)
# graphify install --platform codex

# Web scraping & AI agents (pinned to secure commit SHAs / releases)
git+https://github.com/D4Vinci/Scrapling.git@735af8f2210ed970c99737d4c8126fe4f2ae23a2
# hermes-agent is intentionally excluded from runtime requirements until upstream
# relaxes its exact python-dotenv==1.2.1 pin, which is vulnerable to CVE-2026-28684.
# Optional reference: git+https://github.com/nousresearch/hermes-agent.git@v2026.5.16

# ── New Modules (Phase 1-2) ──
# Volatility Surface (Phase 1.1): ssvi.py is pure Python; no extra deps needed beyond torch+numpy+scipy

# Earnings Analyzer (Phase 1.2) — optional; install if scraping earnings calls
# earnings-analyzer>=1.2.2

# Chart Pattern Detection (Phase 1.3) — optional for YOLOv8 inference
# ultralytics>=8.0.0
# mplfinance>=0.12.0

# Sentiment Ensemble (Phase 1.4) — FinBERT/FinancialBERT via transformers (already present)
# nltk  # for VADER sentiment
# textblob  # optional lightweight sentiment

# Portfolio Optimization (Phase 2.2) — optional
# scikit-portfolio>=0.2.0
# cvxpy>=1.3.0
# Riskfolio-Lib>=3.0.0

# RL Hedging (Phase 2.1) — optional; Stable-Baselines3 for RL agents
# stable-baselines3>=2.0.0
# gymnasium>=0.28.0

# Macro Intelligence (Phase 2.4): pure Python; FRED API via requests (already present)
