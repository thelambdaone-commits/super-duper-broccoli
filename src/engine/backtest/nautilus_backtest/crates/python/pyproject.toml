[build-system]
requires = ["maturin>=1.12,<2"]
build-backend = "maturin"

[project]
name = "python"
version = "0.1.0"
requires-python = ">=3.13"

[tool.maturin]
module-name = "prediction_market_extensions._native_ext"
python-source = "../.."
