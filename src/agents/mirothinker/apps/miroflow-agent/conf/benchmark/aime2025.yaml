# conf/benchmark/aime2025.yaml
defaults:
  - default
  - _self_

name: "aime2025"

data:
  data_dir: "../../data/aime2025"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1