# conf/benchmark/seal-0.yaml
defaults:
  - default
  - _self_

name: "seal-0"

data:
  data_dir: "../../data/seal-0"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1