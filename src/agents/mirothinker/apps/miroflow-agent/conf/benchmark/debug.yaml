# conf/benchmark/debug.yaml
defaults:
  - default
  - _self_

name: "debug"

data:
  data_dir: "../../data/debug"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1
