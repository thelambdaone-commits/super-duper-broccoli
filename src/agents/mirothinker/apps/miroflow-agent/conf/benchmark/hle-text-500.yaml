# conf/benchmark/hle-text-500.yaml
defaults:
  - default
  - _self_

name: "hle-text-500"

data:
  data_dir: "../../data/hle-text-500"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1