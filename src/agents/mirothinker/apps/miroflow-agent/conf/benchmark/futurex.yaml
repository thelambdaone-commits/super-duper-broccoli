# conf/benchmark/futurex.yaml
defaults:
  - default
  - _self_

name: "futurex"

data:
  data_dir: "../../data/futurex"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1
