# conf/benchmark/webwalkerqa.yaml
defaults:
  - default
  - _self_

name: "webwalkerqa"

data:
  data_dir: "../../data/webwalkerqa"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1
