{
  "enabled_env": "OPENVIKING_ENABLED",
  "url_env": "OPENVIKING_URL",
  "api_key_env": "OPENVIKING_API_KEY",
  "account_env": "OPENVIKING_ACCOUNT",
  "user_env": "OPENVIKING_USER",
  "default_url": "http://127.0.0.1:1933",
  "search_endpoints": [
    "/api/v1/search/search",
    "/api/v1/search/find",
    "/api/v1/search/grep",
    "/api/v1/search/glob"
  ],
  "purpose": "Optional search-backed context backend for compact project memory retrieval.",
  "guardrails": [
    "Treat as optional context infrastructure only",
    "Do not store secrets, ledger data, Telegram raw messages, or private logs",
    "Keep trading logic independent from server availability",
    "Prefer local project memory and context cards when disabled or unreachable"
  ]
}
