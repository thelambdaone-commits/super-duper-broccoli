[pytest]
asyncio_mode = auto
markers =
    unit: fast unit-level checks with no external services
    integration: cross-module integration checks
    security: security and access-control regression checks
    slow: long-running tests or tests with heavier fixtures
filterwarnings =
    ignore::DeprecationWarning:websockets.*
    ignore::DeprecationWarning:pkg_resources.*
